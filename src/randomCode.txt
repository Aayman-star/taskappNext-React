This file contains code which is functional so worth saving but passed over for some advanced code

{/* <button
          type="submit"
          className="m-2 px-8 py-2 border-2 border-gray-200 rounded-md shadow-lg"
          onClick={handleClick}>
          Add Task
        </button> */}